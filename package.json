{
  "name": "allgreeks-explorer",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "wrangler pages deploy dist --project-name allgreeks --branch main"
  },
  "devDependencies": {
    "vite": "^6.2.0",
    "wrangler": "^4.10.0"
  }
}
